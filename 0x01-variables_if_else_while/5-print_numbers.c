#include <stdio.h>
/**
 * main - print the last digit of the number stored
 *
 * Return: Always 0.
 */
int main(void)
{
	int n;

	for (n <= 10; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
